<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.file.mapper.FileRecordMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.file.entity.FileRecord">
        <id column="id" property="id"/>
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="file_size" property="fileSize"/>
        <result column="file_type" property="fileType"/>
        <result column="biz_type" property="bizType"/>
        <result column="biz_id" property="bizId"/>
        <result column="uploader_id" property="uploaderId"/>
        <result column="uploader_name" property="uploaderName"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, file_name, file_path, file_size, file_type, biz_type, biz_id,
               uploader_id, uploader_name, created_at
        FROM file_record
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO file_record (file_name, file_path, file_size, file_type, biz_type, biz_id,
                                 uploader_id, uploader_name)
        VALUES (#{fileName}, #{filePath}, #{fileSize}, #{fileType}, #{bizType}, #{bizId},
                #{uploaderId}, #{uploaderName})
    </insert>

    <delete id="deleteById">
        DELETE FROM file_record WHERE id = #{id}
    </delete>

</mapper>
