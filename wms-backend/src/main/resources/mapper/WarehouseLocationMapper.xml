<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.warehouse.mapper.WarehouseLocationMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.warehouse.entity.WarehouseLocation">
        <id column="id" property="id"/>
        <result column="shelf_id" property="shelfId"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="capacity" property="capacity"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByShelfId" resultMap="BaseResultMap">
        SELECT id, shelf_id, code, name, type, capacity, status, deleted, created_at, updated_at
        FROM warehouse_location
        WHERE shelf_id = #{shelfId} AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, shelf_id, code, name, type, capacity, status, deleted, created_at, updated_at
        FROM warehouse_location
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="findByShelfIdAndCode" resultMap="BaseResultMap">
        SELECT id, shelf_id, code, name, type, capacity, status, deleted, created_at, updated_at
        FROM warehouse_location
        WHERE shelf_id = #{shelfId} AND code = #{code} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO warehouse_location (shelf_id, code, name, type, capacity, status)
        VALUES (#{shelfId}, #{code}, #{name}, #{type}, #{capacity}, #{status})
    </insert>

    <update id="update">
        UPDATE warehouse_location
        <set>
            <if test="code != null">code = #{code},</if>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="capacity != null">capacity = #{capacity},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE warehouse_location SET deleted = 1 WHERE id = #{id}
    </update>

    <update id="deleteByShelfId">
        UPDATE warehouse_location SET deleted = 1 WHERE shelf_id = #{shelfId}
    </update>

</mapper>
