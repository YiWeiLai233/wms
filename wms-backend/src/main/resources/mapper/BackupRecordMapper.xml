<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.system.mapper.BackupRecordMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.system.entity.BackupRecord">
        <id column="id" property="id"/>
        <result column="backup_type" property="backupType"/>
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="file_size" property="fileSize"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT id, backup_type, file_name, file_path, file_size, status, remark, created_at
        FROM backup_record
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, backup_type, file_name, file_path, file_size, status, remark, created_at
        FROM backup_record
        WHERE id = #{id}
    </select>

    <select id="findLastBackupTime" resultType="java.time.LocalDateTime">
        SELECT MAX(created_at)
        FROM backup_record
        WHERE status = 'SUCCESS'
        <if test="backupType != null">
            AND backup_type = #{backupType}
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO backup_record (backup_type, file_name, file_path, file_size, status, remark)
        VALUES (#{backupType}, #{fileName}, #{filePath}, #{fileSize}, #{status}, #{remark})
    </insert>

    <delete id="deleteById">
        DELETE FROM backup_record WHERE id = #{id}
    </delete>

</mapper>
