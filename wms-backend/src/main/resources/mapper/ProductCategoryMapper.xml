<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.product.mapper.ProductCategoryMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.product.entity.ProductCategory">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT id, name, parent_id, sort_order, status, created_at, updated_at
        FROM product_category
        WHERE status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, name, parent_id, sort_order, status, created_at, updated_at
        FROM product_category
        WHERE id = #{id}
    </select>

    <select id="findByParentId" resultMap="BaseResultMap">
        SELECT id, name, parent_id, sort_order, status, created_at, updated_at
        FROM product_category
        WHERE parent_id = #{parentId} AND status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_category (name, parent_id, sort_order, status)
        VALUES (#{name}, #{parentId}, #{sortOrder}, #{status})
    </insert>

    <update id="update">
        UPDATE product_category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM product_category WHERE id = #{id}
    </delete>

</mapper>
