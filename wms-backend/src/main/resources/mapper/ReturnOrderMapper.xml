<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.returns.mapper.ReturnOrderMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.returns.entity.ReturnOrder">
        <id column="id" property="id"/>
        <result column="return_no" property="returnNo"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="platform_order_no" property="platformOrderNo"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="warehouse_name" property="warehouseName"/>
        <result column="status" property="status"/>
        <result column="reason" property="reason"/>
        <result column="tracking_no" property="trackingNo"/>
        <result column="shipping_fee" property="shippingFee"/>
        <result column="remark" property="remark"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="deleted" property="deleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByPage" resultMap="BaseResultMap">
        SELECT ro.id, ro.return_no, ro.order_id, ro.order_no,
               so.platform_order_no AS platform_order_no,
               ro.warehouse_id,
               w.name AS warehouse_name, ro.status, ro.reason, ro.tracking_no, ro.shipping_fee, ro.remark,
               ro.operator_id, ro.operator_name, ro.deleted, ro.created_at, ro.updated_at
        FROM return_order ro
        LEFT JOIN sales_order so ON ro.order_id = so.id AND so.deleted = 0
        LEFT JOIN warehouse w ON ro.warehouse_id = w.id
        WHERE ro.deleted = 0
        <if test="returnNo != null and returnNo != ''">
            AND ro.return_no LIKE CONCAT('%', #{returnNo}, '%')
        </if>
        <if test="orderNo != null and orderNo != ''">
            AND ro.order_no LIKE CONCAT('%', #{orderNo}, '%')
        </if>
        <if test="platformOrderNo != null and platformOrderNo != ''">
            AND so.platform_order_no LIKE CONCAT('%', #{platformOrderNo}, '%')
        </if>
        <if test="status != null and status != ''">
            AND ro.status = #{status}
        </if>
        <if test="warehouseId != null">
            AND ro.warehouse_id = #{warehouseId}
        </if>
        ORDER BY id DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT ro.id, ro.return_no, ro.order_id, ro.order_no,
               so.platform_order_no AS platform_order_no,
               ro.warehouse_id,
               w.name AS warehouse_name, ro.status, ro.reason, ro.tracking_no, ro.shipping_fee, ro.remark,
               ro.operator_id, ro.operator_name, ro.deleted, ro.created_at, ro.updated_at
        FROM return_order ro
        LEFT JOIN sales_order so ON ro.order_id = so.id AND so.deleted = 0
        LEFT JOIN warehouse w ON ro.warehouse_id = w.id
        WHERE ro.id = #{id} AND ro.deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO return_order (return_no, order_id, order_no, warehouse_id, status, reason, tracking_no, shipping_fee, remark)
        VALUES (#{returnNo}, #{orderId}, #{orderNo}, #{warehouseId}, #{status}, #{reason}, #{trackingNo}, #{shippingFee}, #{remark})
    </insert>

    <update id="updateStatus">
        UPDATE return_order SET status = #{status} WHERE id = #{id} AND deleted = 0
    </update>

    <select id="findLatestByOrderId" resultMap="BaseResultMap">
        SELECT ro.id, ro.return_no, ro.order_id, ro.order_no,
               so.platform_order_no AS platform_order_no,
               ro.warehouse_id,
               w.name AS warehouse_name, ro.status, ro.reason, ro.tracking_no, ro.shipping_fee, ro.remark,
               ro.operator_id, ro.operator_name, ro.deleted, ro.created_at, ro.updated_at
        FROM return_order ro
        LEFT JOIN sales_order so ON ro.order_id = so.id AND so.deleted = 0
        LEFT JOIN warehouse w ON ro.warehouse_id = w.id
        WHERE ro.order_id = #{orderId} AND ro.deleted = 0 AND ro.status != 'CANCELLED'
        ORDER BY ro.id DESC
        LIMIT 1
    </select>

    <select id="findByOrderId" resultMap="BaseResultMap">
        SELECT ro.id, ro.return_no, ro.order_id, ro.order_no,
               so.platform_order_no AS platform_order_no,
               ro.warehouse_id,
               w.name AS warehouse_name, ro.status, ro.reason, ro.tracking_no, ro.shipping_fee, ro.remark,
               ro.operator_id, ro.operator_name, ro.deleted, ro.created_at, ro.updated_at
        FROM return_order ro
        LEFT JOIN sales_order so ON ro.order_id = so.id AND so.deleted = 0
        LEFT JOIN warehouse w ON ro.warehouse_id = w.id
        WHERE ro.order_id = #{orderId} AND ro.deleted = 0
        ORDER BY ro.id ASC
    </select>

</mapper>
