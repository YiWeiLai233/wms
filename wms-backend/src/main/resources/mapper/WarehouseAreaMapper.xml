<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.warehouse.mapper.WarehouseAreaMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.warehouse.entity.WarehouseArea">
        <id column="id" property="id"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByWarehouseId" resultMap="BaseResultMap">
        SELECT id, warehouse_id, code, name, type, status, deleted, created_at, updated_at
        FROM warehouse_area
        WHERE warehouse_id = #{warehouseId} AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, warehouse_id, code, name, type, status, deleted, created_at, updated_at
        FROM warehouse_area
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="findByWarehouseIdAndCode" resultMap="BaseResultMap">
        SELECT id, warehouse_id, code, name, type, status, deleted, created_at, updated_at
        FROM warehouse_area
        WHERE warehouse_id = #{warehouseId} AND code = #{code} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO warehouse_area (warehouse_id, code, name, type, status)
        VALUES (#{warehouseId}, #{code}, #{name}, #{type}, #{status})
    </insert>

    <update id="update">
        UPDATE warehouse_area
        <set>
            <if test="code != null">code = #{code},</if>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE warehouse_area SET deleted = 1 WHERE id = #{id}
    </update>

    <update id="deleteByWarehouseId">
        UPDATE warehouse_area SET deleted = 1 WHERE warehouse_id = #{warehouseId}
    </update>

</mapper>
