<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.ai.mapper.AiConversationMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.ai.entity.AiConversation">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_conversation (user_id, title)
        VALUES (#{userId}, #{title})
    </insert>

    <select id="findByIdAndUserId" resultMap="BaseResultMap">
        SELECT id, user_id, title, created_at, updated_at
        FROM ai_conversation
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, title, created_at, updated_at
        FROM ai_conversation
        WHERE user_id = #{userId}
        ORDER BY updated_at DESC, id DESC
    </select>

    <update id="touch">
        UPDATE ai_conversation
        SET updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteByIdAndUserId">
        DELETE FROM ai_conversation
        WHERE id = #{id} AND user_id = #{userId}
    </delete>
</mapper>
