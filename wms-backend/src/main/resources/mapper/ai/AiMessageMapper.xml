<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.ai.mapper.AiMessageMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.ai.entity.AiMessage">
        <id column="id" property="id"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="user_id" property="userId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="metadata" property="metadata"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_message (conversation_id, user_id, role, content, metadata)
        VALUES (#{conversationId}, #{userId}, #{role}, #{content}, #{metadata})
    </insert>

    <select id="findByConversationId" resultMap="BaseResultMap">
        SELECT id, conversation_id, user_id, role, content, metadata, created_at
        FROM ai_message
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at ASC, id ASC
    </select>

    <delete id="deleteByConversationId">
        DELETE FROM ai_message
        WHERE conversation_id = #{conversationId}
    </delete>
</mapper>
