<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.log.mapper.OperationLogMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.log.entity.OperationLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="operation" property="operation"/>
        <result column="method" property="method"/>
        <result column="params" property="params"/>
        <result column="result" property="result"/>
        <result column="status" property="status"/>
        <result column="error_msg" property="errorMsg"/>
        <result column="ip" property="ip"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findByPage" resultMap="BaseResultMap">
        SELECT id, user_id, user_name, operation, method, params, result,
               status, error_msg, ip, created_at
        FROM operation_log
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="operation != null and operation != ''">
                AND operation = #{operation}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (user_id, user_name, operation, method, params, result,
                                   status, error_msg, ip)
        VALUES (#{userId}, #{userName}, #{operation}, #{method}, #{params}, #{result},
                #{status}, #{errorMsg}, #{ip})
    </insert>

</mapper>
