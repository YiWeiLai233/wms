<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiweilai.wms.log.mapper.OperationLogMapper">

    <resultMap id="BaseResultMap" type="com.yiweilai.wms.log.entity.OperationLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="module" property="module"/>
        <result column="action" property="action"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="detail" property="detail"/>
        <result column="ip" property="ip"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findByPage" resultMap="BaseResultMap">
        SELECT id, user_id, user_name, module, action, target_type, target_id,
               detail, ip, created_at
        FROM operation_log
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="module != null and module != ''">
                AND module = #{module}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (user_id, user_name, module, action, target_type,
                                   target_id, detail, ip)
        VALUES (#{userId}, #{userName}, #{module}, #{action}, #{targetType},
                #{targetId}, #{detail}, #{ip})
    </insert>

</mapper>
